# UniPPT — 性能测评

[English](README.md) | **简体中文**

2026-09-16 在 Windows 10 / Intel Core i7-1165G7 / 31.70 GiB 内存上实测。每项预热 2 次、测量 7 次，顺序执行。

## 测试环境

| 字段 | 记录 |
| --- | --- |
| OS | Microsoft Windows 10 专业版 (10.0.19045, AMD64) |
| CPU | 11th Gen Intel(R) Core(TM) i7-1165G7 @ 2.80GHz (4 cores / 8 threads) |
| RAM | 31.70 GiB |
| Rust | `rustc 1.97.1 (8bab26f4f 2026-07-14)` |
| Python | 3.13.11 |
| 构建源提交 | `e66c1e0eab10a93afd776a755e0973149dedcd73` |
| 构建配置 | `cargo build --release --locked -p unippt-server; default opt-level=3, lto=thin, strip=symbols` |
| 程序大小 | 9,549,824 bytes |
| 测量时间 UTC | 2026-09-15T22:52:30.728319+00:00 |

## 工作负载与计时边界

每页包含十个文本框与十个矩形，不包含图片、公式、字体文件、动画或外部资源。导出输入不带源文件缓存 ID；每次导入使用不同内容的生成文件，并断言服务缓存头为 `miss`。导入后核对全部页数、对象数量和文本内容。

HTTP 计时包含请求传输、服务处理和完整响应读取；输入生成与结果检查在计时区间外。服务在测量前已启动，不包含浏览器渲染。

## 完整结果

| 操作 | 规模（页） | 中位数 ms | P95 ms |
| --- | ---: | ---: | ---: |
| PPTX 导出 | 10 | 21.96 | 26.99 |
| PPTX 导入，缓存未命中 | 10 | 180.58 | 273.25 |
| UDOC 导出 | 10 | 147.40 | 316.19 |
| UDOC 导入，缓存未命中 | 10 | 10.69 | 10.71 |
| HTML 导出 | 10 | 39.81 | 41.13 |
| HTML 导入，缓存未命中 | 10 | 10.74 | 14.22 |
| PPTX 导出 | 50 | 64.09 | 120.46 |
| PPTX 导入，缓存未命中 | 50 | 928.32 | 1080.34 |
| UDOC 导出 | 50 | 500.70 | 892.52 |
| UDOC 导入，缓存未命中 | 50 | 35.75 | 41.94 |
| HTML 导出 | 50 | 121.08 | 148.14 |
| HTML 导入，缓存未命中 | 50 | 37.79 | 78.62 |
| PPTX 导出 | 100 | 139.41 | 316.91 |
| PPTX 导入，缓存未命中 | 100 | 813.36 | 1721.34 |
| UDOC 导出 | 100 | 669.36 | 1189.64 |
| UDOC 导入，缓存未命中 | 100 | 72.49 | 85.58 |
| HTML 导出 | 100 | 325.71 | 369.41 |
| HTML 导入，缓存未命中 | 100 | 83.82 | 278.04 |

[原始数据](results/2026-09-16-windows-x64.json) · [测评脚本](run.py)

## 统计方法与限制

预热样本不计入统计。中位数为第 4 个排序样本；P95 使用最近秩法 `ceil(0.95 × n)`，在 n=7 时等于本次最大值，不能视为稳定尾延迟估计。原始 JSON 保留全部观测值、输入/输出大小或哈希、校验结果、二进制与脚本 SHA-256。此处的源提交是构建时运行时代码的版本；本次新增文档与脚本未修改运行时代码。

测试在共享工作站上进行，未控制所有后台负载、功耗或文件缓存状态；没有剔除慢样本，也没有执行并发压力测试。结果仅适用于这些合成工作负载；未测峰值内存、完整应用启动时间、浏览器帧率、真实复杂文档、外部 AI 和云服务。Microsoft 365（Office 365）与 WPS Office 本轮未实测，不能从这些数据推导相对速度排名。

## 复现

```sh
cargo build --release --locked -p unippt-server
python benchmarks/run.py --binary target/release/unippt-server --sizes 10,50,100 --warmups 2 --samples 7 --output benchmarks/results/local.json
python benchmarks/verify_results.py
```

Windows 下为程序路径追加 `.exe`；若使用 `--target-dir`，应相应修改 `--binary`。脚本只使用 Python 标准库，输入完全由脚本生成；服务型测评会启动和关闭自己的临时回环服务进程。使用相同源码、锁文件、编译器与构建配置复现；时间因机器与负载而异。

[Office 365 / WPS 对照与后续同机测试协议](../docs/COMPARISON.zh-CN.md)
